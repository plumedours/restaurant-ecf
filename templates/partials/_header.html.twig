<nav class="bg-slate-900 px-2 sm:px-4 py-2.5">
  <div class="container flex flex-wrap items-center justify-between md:justify-evenly lg:justify-between mx-auto">
    <a href="{{path('app_home')}}" class="flex items-center">
        <img src="{{asset('assets/images/quai_antique_logo.png')}}" class="h-20 md:h-28" alt="Logo" />
    </a>
    <button data-collapse-toggle="navbar-default" type="button" class="inline-flex items-center p-2 ml-3 text-sm text-slate-300 rounded-lg md:hidden hover:bg-slate-800" aria-controls="navbar-default" aria-expanded="false">
      <span class="sr-only">Open main menu</span>
      <span class="material-symbols-outlined">menu</span>
    </button>
    <div class="hidden w-full md:block md:w-auto" id="navbar-default">
      <ul class="flex flex-col items-start md:items-center p-4 mt-4 text-slate-300 md:flex-row md:space-x-8 md:mt-0 md:text-sm lg:text-lg md:font-medium md:border-0">
        <li>
          <a href="{{path('app_home')}}" class="block py-2 pl-3 pr-4 hover:bg-slate-800 md:bg-transparent md:border-0 md:hover:text-yellow-600 md:p-0" aria-current="page">Accueil</a>
        </li>
        <li>
          <a href="{{path('app_menu')}}" class="block py-2 pl-3 pr-4 hover:bg-slate-800 md:hover:bg-transparent md:border-0 md:hover:text-yellow-600 md:p-0">Carte</a>
        </li>
        <li>
          <a href="{{path('app_book')}}" class="block py-2 pl-3 pr-4 hover:bg-slate-800 md:hover:bg-transparent md:border-0 md:hover:text-yellow-600 md:p-0">Réserver une table</a>
        </li>
        <li>
          <a href="{{path('app_contact')}}" class="block py-2 pl-3 pr-4 hover:bg-slate-800 md:hover:bg-transparent md:border-0 md:hover:text-yellow-600 md:p-0">Contact</a>
        </li>
        {% if is_granted('ROLE_USER') %}
        <li>
          <span data-dropdown-toggle="userDropdown" data-dropdown-placement="bottom-start" class="material-symbols-outlined text-3xl cursor-pointer hover:text-yellow-800">account_circle</span>
        </li>
        <!-- Dropdown menu -->
        <div id="userDropdown" class="hidden z-10 w-44 bg-white rounded divide-y divide-gray-100 shadow dark:bg-gray-700 dark:divide-gray-600">
            <div class="py-3 px-4 text-sm text-gray-900 dark:text-white">
              <div>{{ app.user.firstname ~ ' ' ~ app.user.lastname}}</div>
              <div class="font-medium truncate">{{ app.user.email}}</div>
            </div>
            <ul class="py-1 text-sm text-gray-700 dark:text-gray-200" aria-labelledby="avatarButton">
              {% if is_granted('ROLE_ADMIN') %}
              <li>
                <a href="{{path('admin')}}" class="block py-2 px-4 hover:bg-gray-100 dark:hover:bg-gray-600 dark:hover:text-white">Dashboard</a>
              </li>
              {% endif %}
              <li>
                <a href="{{path('app_profile')}}" class="block py-2 px-4 hover:bg-gray-100 dark:hover:bg-gray-600 dark:hover:text-white">Mon espace</a>
              </li>
            </ul>
            <div class="py-1">
              <a href="{{path('app_logout')}}" class="block py-2 px-4 text-sm text-gray-700 hover:bg-gray-100 dark:hover:bg-gray-600 dark:text-gray-200 dark:hover:text-white">Déconnexion</a>
            </div>
        </div>
        {% else %}
        <li>
          <a href="{{path('app_login')}}" class="block py-2 pl-3 pr-4 rounded hover:bg-slate-800 md:hover:bg-transparent md:border-0 md:hover:text-yellow-600 md:p-0">Connexion</a>
        </li>
        {% endif %}
      </ul>
    </div>
  </div>
</nav>